---
"svsch": minor
---

Add `svsch.clockSignalNames` and `svsch.resetSignalNames` settings so the case-insensitive substrings used to recognize clock/reset signals for the auto-cut-on-open feature are configurable instead of hardcoded to `clk`/`clock` and `rst`/`reset`.
